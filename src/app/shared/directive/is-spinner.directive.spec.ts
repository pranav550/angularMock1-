import { IsSpinnerDirective } from './is-spinner.directive';

describe('IsSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new IsSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
